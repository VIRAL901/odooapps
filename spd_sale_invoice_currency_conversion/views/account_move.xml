<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="inherit_account_move_form" model="ir.ui.view">
        <field name="name">inherit.account.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="delivery_date" position="after">
                <field name="conversion_currency_id"
                       options="{'no_create':True,'no_create_edit':True}"/>
                <field name="conversion_rate"/>
                <!-- Don't Remove this field -->
                <field name="conversion_amount_total" invisible="1"/>
            </field>
        </field>
    </record>

    <record id="inherit_account_move_tree" model="ir.ui.view">
        <field name="name">inherit.account.move.tree</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <field name="amount_total_signed" position="after">
                <field name="conversion_amount_total" />
            </field>
        </field>
    </record>
</odoo>
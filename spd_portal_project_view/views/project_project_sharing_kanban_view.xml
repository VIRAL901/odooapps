<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="project_sharing_portal" name="My Project">
        <t t-set="pageName" t-value="'o_project_sharing_container'"/>
        <t t-set="no_footer" t-value="true"/>
        <t t-call="spd_portal_project_view.project_project_sharing"/>
    </template>

    <template id="project_project_sharing" name="Project Sharing View">
        <iframe style="width: -webkit-fill-available; height: -webkit-fill-available;" class="flex-grow-1"
                frameborder="0" t-attf-src="/my/projects/project_sharing"/>
    </template>

    <template id="project_project_sharing_embed" name="Project Sharing View Embed">
        <t t-call="web.layout">
            <t t-set="head_project_sharing">
                <script type="text/javascript">
                    odoo.__session_info__ = <t t-out="json.dumps(session_info)"/>;
                    // Prevent the menu_service to load anything. In an ideal world, Project Sharing assets would only contain
                    // what is genuinely necessary, and not the whole backend.
                    odoo.loadMenusPromise = Promise.resolve();
                </script>
                <base target="_parent"/>
                <t t-call-assets="spd_portal_project_view.webclient"/>
            </t>
            <t t-set="head" t-value="head_project_sharing + (head or '')"/>
            <t t-set="body_classname" t-value="'o_web_client o_project_sharing'"/>
        </t>
    </template>

</odoo>


<?xml version="1.0" encoding="utf-8"?>
<template xml:space="preserve">
    <t t-name="attendance_menu" t-inherit="hr_attendance.attendance_menu" t-inherit-mode="extension" owl="1">
        <xpath expr="//i[hasclass('fa-circle')]" position="replace">
            <i class="fa fa-circle" t-on-click="() => this.onClickVideoOn()"
               t-attf-class="text-{{ this.state.checkedIn ? 'success' : 'danger' }}" role="img"
               aria-label="Attendance"/>
        </xpath>
        <xpath expr="//div[hasclass('o_att_menu_container')]" position="replace">
            <div class="o_att_menu_container d-flex flex-column">
                <t t-if="this.bcameraAccess">
                    <img src=""/>
                    <canvas width="0" height="0" id="mycanvas" style="display: none;"></canvas>
                    <div id="output"></div>
                    <div id="screenshot" style="text-align:center;">
                        <h6 id="error_handle_msg"></h6>
                        <video class="videostream" muted="" playsinline="" autoplay=""></video>
                        <img id="screenshot-img" src="data:," style="display: none;"/>
                    </div>
                </t>
                <div class="d-flex flex-column gap-3">
                    <div t-if="this.state.checkedIn" class="d-flex flex-wrap gap-3">
                        <div t-if="!this.isFirstAttendance" class="att_container flex-grow-1 flex-shrink-0">
                            <small class="d-block text-muted">Before <t t-esc="this.lastCheckIn"/></small>
                            <div t-esc="this.hoursPreviouslyToday" class="fs-3 text-info text-end"/>
                        </div>
                        <div class="att_container flex-grow-1 flex-shrink-0">
                            <small class="d-block text-muted">Since <t t-esc="this.lastCheckIn"/></small>
                            <div t-esc="this.lastAttendanceWorkedHours"
                                 t-attf-class="fs-3 text-info {{ !this.isFirstAttendance ? 'text-end' : '' }}"/>
                        </div>
                    </div>
                        <div t-if="!this.isFirstAttendance"
                             class="att_container d-flex flex-column"
                             t-att-class="this.state.checkedIn ? 'p-3 bg-100 rounded' : ''">
                        <div class="d-flex"
                             t-att-class="this.state.checkedIn ? 'align-items-center justify-content-between' : 'flex-column'">
                            <small class="text-muted">Total today</small>
                            <h2 t-esc="this.hoursToday" class="mb-0 fs-2"/>
                        </div>
                            <button t-on-click="() => this.onClickSignInOut()" class="flex-basis-100 mt-3"
                                    t-attf-class="btn btn-{{ this.state.checkedIn ? 'warning' : 'success' }}">
                            <span t-if="!this.state.checkedIn">Check in</span>
                                <span t-else="">Check out</span>
                                <i t-attf-class="fa fa-sign-{{ this.state.checkedIn ? 'out' : 'in' }} ms-1"/>
                        </button>
                    </div>
                </div>
                <button t-if="this.isFirstAttendance" t-on-click="() => this.onClickSignInOut()"
                            t-attf-class="btn btn-{{ this.state.checkedIn ? 'warning' : 'success' }}">
                    <span t-if="!this.state.checkedIn">Check in</span>
                        <span t-else="">Check out</span>
                        <i t-attf-class="fa fa-sign-{{ this.state.checkedIn ? 'out' : 'in' }} ms-1"/>
                </button>
            </div>
        </xpath>
      </t>
</template>
